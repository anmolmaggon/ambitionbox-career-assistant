# AmbitionBox CEO Demo V3 agent entry

For every task inside this project:

1. Read and follow `.agents/skills/ambitionbox-v3-workflow/SKILL.md`.
2. Read `context/START_HERE.md` and the files it routes to.
3. Work only inside `ceo-demov3/`. Every sibling project and root-level project file is frozen.

For any user-facing wording, pitch, naming, CTA, or trust-language change, also read and follow `.agents/skills/ambitionbox-v3-copywriting/SKILL.md`.

Current ownership:

- Codex: Onboarding through the documented Home handoff.
- Claude: Home.
- Shared foundations: change only when explicitly included in the user's requested layer.

Explicit feedback in the current conversation overrides the saved context. Nothing becomes locked unless the owner explicitly says **lock**.
