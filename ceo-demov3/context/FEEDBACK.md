# Owner Feedback and Durable Decisions

This is an append-only learning record. New feedback may supersede an older rule; do not silently delete history.

| Area | Owner feedback or decision | Durable rule | Status |
|---|---|---|---|
| Naming | Do not introduce “Career Assistant” during onboarding. | Brand the experience simply as AmbitionBox. | Active |
| Scope | The immediate experience is about job search. | Do not inflate the first-open promise into the user's entire career. | Active |
| Promise | “Every opportunity” sounds indiscriminate. | Emphasize the opportunity that matches the user and deserves their best shot. | Active |
| Product value | AmbitionBox also drafts, fills résumé gaps, guides action, and supports negotiation. | Present guidance and execution support, not decision support alone. | Active |
| Transparency | Users need truth about pay, ratings, policy, and employee reality. | Pair personalization with evidence so users feel informed and protected from a bad decision. | Active |
| Tone | Emotional and direct copy is preferred. | Use emotional directness for the promise and specific product language afterward. | Active |
| First open | Yazio and Slopes create a welcome moment before feature explanation. | Give the first open a human, spacious promise; do not begin as a dense product dashboard. | Active |
| Structure | A carousel is no longer wanted. | Use one combined welcome-and-login hero. Legacy carousel routes resolve to it. | Active |
| Naukri | A detected Naukri account makes autofill credible and useful. | Make simulated Naukri detection the golden path; show the person and autofill benefit, followed by review. | Active |
| Recognised identity | AmbitionBox and Naukri share a parent company, so the recognised Naukri name and email may be shown before import. | Preserve the identity-led one-tap experience; keep the demo integration explicitly simulated without weakening the hero copy. | Active |
| Alternatives | Google and Apple must remain available. | Keep both visible and clarify that authentication does not connect an inbox. | Active |
| Layout references | Shop demonstrates bold copy plus a concrete product example; Cuvva demonstrates concise icon-led benefits. | Use references for hierarchy and proof patterns, not direct visual cloning. | Active |
| Illustration | Structure should be locked before imagery. The generated 3D results felt poor. | Do not add illustration until hierarchy is sound; reject generic 3D job-search objects and cosmetic AI gradients. | Active |
| Hero character | The first hero felt characterless because pitch and authentication occupied the same undifferentiated white canvas. | Give the pitch and login distinct visual worlds, using a confident AmbitionBox colour field above a quiet white authentication area. | Active |
| Hero copy density | The three-line headline repeated the three benefit points; bridge, import-status, review reassurance, and sign-in reassurance added clutter. | Let the three value points carry the pitch. Remove redundant headline and helper layers from the first-open screen. | Active |
| Authentication shape | Provider actions should feel generous and inviting. | Use large pill-shaped CTAs for Naukri, Google, and Apple on first open. | Active |
| First-open refinement | “Most” weakens the first benefit; autofill needs more emphasis; the placeholder Naukri lettermark is not acceptable. | Say “See the roles worth your time,” treat autofill as a compact highlighted USP, and use the canonical Naukri symbol for the provider action. | Active |
| Onboarding header | Use the compact mobile header pattern with back, staged progress, and close; it may sit directly on a blue hero. | Keep one reusable header structure across onboarding, changing its surface treatment rather than its anatomy. | Active |
| Progress feedback | Completed import steps should feel visibly resolved. | Animate a completion tick once as a step becomes done; reduced motion shows the completed state immediately. | Active |
| Onboarding progress | The six-dot header felt hard-coded, and manual setup through Google should visibly have more steps. | Derive progress from semantic branch plans: four steps for imported profiles, eight for manual setup, and no determinate progress before a profile source is chosen. Keep local import/scan progress separate. | Active |
| Profile review | Imported profile data should feel reviewable and connected to its source, not like a generic settings list. | Keep one continuous grouped surface, place source provenance inside it, use a compact matrix for short facts, and reserve full-width rows for longer content such as skills and résumé. | Active |
| Naukri import scope | Naukri can provide the complete profile: name, résumé, profile summary, recognitions, education, skills, experience, current professional details, and preferences; take all of it except gender. | Treat Naukri as a comprehensive career-profile transfer. Exclude gender, preserve source provenance, and review a rich profile by exception instead of giving every field equal weight. This supersedes the thin seven-field review assumption. | Active |
| Profile review correction | The Naukri review had unnecessary compensation freshness intelligence, a redundant Profile/Résumé/Preferences summary, and incorrectly called the result a “Naukri profile.” | Keep provenance factual and compact. Call it the user's profile, remove premature intelligence and inventory summaries, and orient the supporting copy toward review. | Active |
| Missing profile data | A field may be absent in Naukri and should be addable during review. | Preserve missing values as empty. Show the exact missing field with an Add action instead of filling or inferring it. | Active |
| Profile review action | “Review job preferences” describes the next screen rather than completing the current one; “Looks good” is ambiguous. | Use **Confirm profile** for Profile Review. | Active |
| CTA shape | All CTAs should be rounded from this point onward. | Primary and secondary CTAs use a full pill radius across V3. | Active |
| Profile icon colour | The Matter reference's colourful category icons improve scanability. | Use restrained, category-specific colour inside profile row icon tiles while preserving one quiet page palette. | Active |
| CTA shape clarification | Some contextual Edit actions still looked like rounded rectangles even after primary buttons became pills. | Every text-labelled CTA uses a full pill radius across V3; icon-only utilities and disclosure rows retain role-appropriate shapes. | Active |
| Profile review type | The Profile Review working text felt too small despite a sufficiently large headline. | Use 14–15px core body and row-title type with 12px metadata; allow scrolling instead of shrinking important content. | Active |
| Profile review delight | The review felt correct but emotionally flat after a fast Naukri import. | Celebrate the real autofill outcome with concise copy and one restrained speed accent; avoid scores, confetti, or unexplained intelligence. | Active |
| Profile provenance structure | The Naukri autofill section felt detached when it floated above the profile widget. | When one source qualifies the whole review artifact, merge provenance into that artifact as an attached header with no card gap. | Active |
| Co-brand provenance | The attached header should show AmbitionBox × Naukri, and the standalone lightning icon felt unnecessary. | Represent first-party transfer with a compact co-brand lockup; let the outcome copy provide delight and remove redundant decorative icons. | Active |
| Missing-field hierarchy | Notice period looked like its own section when shown as a standalone row, while placing every gap at the end would hide it. | Keep missing values visible but attach each one beneath its native section header with an immediate Add action. | Active |
| Imported preferences | The Naukri path already supplies ranking preferences, so presenting five open inputs repeats setup and feels like a generic form. | Review imported preferences as one attached source-aware row surface; open one focused editor only for an exception and use **Confirm preferences** to complete the step. | Active |
| First-open icon character | The white-and-blue pointer icons felt too uniform; the owner asked for colour or emoji. | Use distinct pastel category colour with the established native icon family. Avoid device-dependent emoji when native icons can provide consistent character. | Active |
| First-open storytelling motion | The three first-open pointers should appear one by one and feel like a top-to-bottom flow. | Draw the connection and reveal match, reality, then guidance once in sequence. Never loop; reduced motion shows the complete state immediately. | Active |
| Review value context | Profile and preference review showed what was imported but not clearly enough what confirming it gives the user. | Before detailed review, explain the user outcome. Profile powers matching, gap detection, and guidance; the five preference fields control ranking and remain editable. | Active |
| Profile value density | The three-row “What this profile unlocks” block repeated too much before a dense profile review. | Explain matching, gap checks, and tailored guidance in one strong supporting sentence rather than a second list. | Active |
| Optional preferences | The preference review should support companies, tech stacks, role level, and industries without forcing the user to fill them. | Separate imported preferences from visibly optional refinements; preserve empty values and allow confirmation without completing them. | Active |
| Preference provenance copy | “Five preferences, ready to review” became brittle once optional refinements were introduced. | Use factual source copy that does not depend on a changing field count: **Your job preferences from Naukri** and **Review or change them anytime**. | Active |
| Preference consequence | After confirming preferences, the experience should visibly begin curating roles across job sources before asking for Gmail. | Insert one short automated cross-source Curation beat between Preferences and optional Email; name representative sources, show meaningful progress, and avoid an unverifiable “every job” claim. | Active |
| Profile summary hierarchy | The full imported summary should not remain permanently expanded above the profile section list. | Keep identity visible, then place the professional summary in the first collapsed accordion with its headline as the preview and its edit action inside. | Active |
| Curation focus | The Curation animation moved too quickly and was crowded by a top logo, preference status, bottom checklist, and demo footer. | Sustain one source-network moment while the main copy teaches coverage, fit, and focused effort in sequence. Remove redundant chrome and let the living blue surface collapse behind Email instead of hard-cutting away. | Superseded by Curation canvas |
| Curation canvas | The full dark-blue background made Curation feel too promotional. | Use a white product canvas with dark copy; reserve AmbitionBox blue for the live connection system and transition accents. | Active |
| Curation source consistency | The Naukri logo made one source node visually heavier than the other three. | Use text-only labels for all Curation sources so the network communicates breadth without promoting one provider. | Active |
| Curation orbit | The four job-source tiles should feel like active inputs rather than fixed diagram nodes. | Orbit all source tiles and their connection paths slowly around a fixed AmbitionBox hub; counter-rotate labels for readability and remove the orbit under reduced motion. | Active |
| Email pitch character | The Email Connection screen looked like a polished settings screen rather than an irresistible pitch, and it did not show that relevant-role matching continued in the background. | Treat Email Connection as a second campaign beat: retain one compact live matching status, bridge with **Meanwhile**, use an emotionally direct opportunity consequence, and visually divide the blue pitch world from the white permission and action world. | Superseded by unified Email |
| Email value before permission | The owner wants connected email sold as premium assistance before Gmail or provider choice appears. | Separate value from permission: use one cinematic product-theatre screen to show help across recruiter replies, interviews, and offers, then open the quieter provider and trust screen. Preserve optionality on both surfaces. | Superseded by unified Email |
| Email sequence simplification | Park Curation, remove the separate blue permission screen, and make the premium assistance screen the one email decision. | Open one light unified Email screen after Preferences. Combine proof, Gmail, another email, trust, and optionality without a second pitch or permission screen. This supersedes the separate-value-screen rule. | Active |
| Email action hierarchy | Gmail and another-email actions should be direct, while skip belongs in the header. | Use **Connect Gmail** with the Gmail mark, **Connect another email** as the secondary pill, and one top-right **Skip**. Do not repeat a bottom later action. | Active |
| Email visual theme | The premium pitch should not use a dark canvas. | Keep the unified Email screen on the light AmbitionBox canvas; use product hierarchy, type, native proof, and restrained depth for premium character. | Active |
| Shared copy practice | Codex and Claude need a reusable method informed by *The Copy Book*. | Require the model-neutral copywriting skill for pitches, headlines, CTA, naming, and trust language. Treat its D&AD principles as an attributed synthesis, not a copied or canonical book doctrine. | Active |
| Email headline motivation | “Never miss the moment that moves your search forward” does not make connecting Gmail feel valuable enough. | When asking for inbox access, pair the small input with the specific unlock: one connection enables timely help across recruiter replies, interviews, and offers. Avoid abstract “moment” or “move forward” language. | Active |
| Email assistance promise | The owner selected “Every application. Every step. Unmatched assistance.” and asked for a short supporting line. | Use the selected line as the draft Email headline, follow with one concise reason to connect, and let concrete use cases substantiate “unmatched.” This remains draft until explicitly locked. | Active |
| Email proof specificity | Generic stages such as recruiter reply, interview invite, and offer did not show enough of the assistance. | Demonstrate realistic before-and-after use cases: a notice-period question becomes a reviewable reply, a scheduled system-design round becomes role-specific prep, and an offer becomes negotiation guidance. | Active |
| Email trust density | The inbox disclaimer felt too prominent and “Nothing is sent” incorrectly implied that an approved send could never exist. | Remove the mid-screen trust block. Place **Read-only access. You’re in control.** as one quiet line below both provider actions; preserve explicit review before any future send. | Active |
| Email proof frame | The “Across every application · Your advantage, in action” card header was unnecessary. | Let the three use cases prove the promise unlabelled. Do not caption a proof block with a line that restates the headline. | Active |
| Email headline emphasis | The headline read as three flat statements; the promise needed to lead. | Set **Unmatched assistance.** in the AmbitionBox primary colour, following the established colour-shift emphasis pattern rather than italic or weight. | Active |
| Email ask specificity | The screen did not make it clear that AmbitionBox wants the inbox where job updates actually arrive. | Ask for that inbox in the user's own terms: **Connect the email where your job updates already land.** Avoid the invented category “job-search email”; “already” signals nothing new is required. | Active |
| Post-connection terminology | The account sheet and scan still said “job-search emails” immediately after the Email screen dropped the term. | Carry one vocabulary through the whole connection beat. The sheet asks for the inbox where job updates already land; the scan states scope, not the category. | Active |
| Scan progress duplication | “90 days” appeared three times on the first scan stage — subtitle, stat numeral, and footer label — and the screen ran two progress systems at once. | Let the named four-stage rail be the progress system and the stage stat be the only number. The footer bar stays as an unlabelled continuous cue; it carries no percentage. | Active |
| Scan stage-one scope | The first stage restated its own timespan instead of telling the user what is being read. | Use the first stage to state the boundary — **Only your job-related email—nothing else.** Keep the boundary next to the moment access is used. | Active |
| Import result counts | The supporting line pre-announced 15 and 3, then the stat row delivered the same numbers immediately below. | State the meaning in copy and let the stat row own the counts. Counts appear once per screen. | Active |
| Import result rail | The four-stage Explore→Applications→Interviews→Offer rail taught a journey model that Home had already abandoned as navigation dressed as progress. | Replace it with a receipt of the four scan stages the user just watched. Onboarding closes the scan it opened and plants no journey model for Home to drop. Receipt labels carry no counts; the stat row owns those. | Active |
| Demo simulation notes | The owner removed the amber “Demo simulation—no email account is accessed.” note and asked that it not be used anywhere. | Do not add demo/prototype simulation disclaimers to onboarding surfaces. This supersedes the workflow skill's instruction to preserve simulated-integration disclosures for onboarding; read-only and review-before-send boundaries still stand. | Active |
| Scan CTA shape | **Skip scan** was a bare text button, breaking the rule that every text-labelled CTA uses a full pill radius. | Give Skip scan a pill with a quiet tint. Audit transient/process screens against the CTA shape rule too, not just primary flows. | Active |
| Scan reduced motion | The scan's per-stage transition animated even under reduced motion, unlike every other V3 screen. | Gate the stage transition on useReducedMotion so the complete state renders immediately. | Active |
| Workflow | Models should change only the layer requested. | Establish permitted and protected Product, Copy, UX, and UI layers before editing. | Active |
| Approval | The owner did not want implicit locking. | Only the explicit word “lock” changes status. | Active |
| Collaboration | Codex will handle Onboarding and Claude will handle Home. | Both read one shared context and edit only their owned workstream. | Active |
| Email hero clarity | The owner tested the email screen and could not tell from the hero and supporting line that the screen was asking for email access. | Lead the headline with the imperative ask — **Connect your inbox.** — colour-emphasised, followed by the benefit in the same sentence. Keep the supporting line focused on naming the object and defusing hesitation, not restating the headline's benefit. | Active |
| Naukri import pacing | The Naukri import animation cycled through its three steps too fast to read. | Slow the per-step interval and hold duration so each import step is legible before the screen advances. | Active |
| Onboarding scroll position | Advancing to a new onboarding step (e.g. after Confirm profile) kept the previous screen's scroll position instead of starting at the top. | Reset scroll to top on every onboarding step change. | Active |
| Work mode label | “Light hybrid” read as an invented, oddly qualified category. | Use the plain label **Hybrid** for the work-mode preference everywhere in onboarding. | Active |
| Navigation | Explore should not be a bottom-nav tab; it is similar to the capability sheet, so that list should be merged rather than duplicated. | No Explore tab. The capability index lives in the assistant sheet and is permanent in every direction, not the Composer direction only — the composer is permanent chrome, so the list is reachable from any state without spending a tab. | Active |
| Navigation | Bottom navigation references supplied for review (MOB-HOME-001 Cleo, MOB-HOME-002 Places). | Bottom navigation is Home · Tracker · Matches app-wide, avatar as the door to Profile. Icons with labels, active state expressed as fill, sitting quietly on the canvas rather than in a heavy white bar. Promotion is a shared-foundation change and must update `src/AppUI.jsx` and the hand-rolled copy in `ProfileScreen` together. | Active |
| Home structure | The unified carousel direction was chosen over keeping an adaptive hero above a separate set-aside queue. | Home's body is one ordered swipeable peeking sequence with uniform card sizes and a per-card dismiss. Ranking still comes from `nextBestAction()` and `setAside()`; only the rendering target changes. Supersedes the one-dominant-hero and separate-queue clauses in HOME.md. | Active |
| Home material | Reference screens supplied for neatness, top chat and widgets (Bond, Places, Monzo, Klarna). | Home's canvas follows MOB-HOME-003 (Bond): soft neutral field, white rounded cards, small grey eyebrow labels above bold plain statements, one pill action per card. Atmosphere comes from a quiet canvas and editorial typography, never gradient, glass or sparkle. MOB-HOME-005/006 are read for widget and carousel structure only; their gradients are out of bounds. | Active |
| Home greeting | The greeting said “I found one thing worth acting on now.” while the carousel below it held four cards. | The greeting states the size of the sequence it introduces, counted from the sequence itself rather than authored per state. A screen that claims to have seen everything cannot miscount what it is showing. | Active |
| Home card fields | The prep-v3 violet interview field and the green offer field were readable as a hero, not as one card among equals. | Every carousel card is the same white Bond surface. A sequence of equals cannot have one card wearing a different field without re-introducing the hero the rebuild removed; the beat stays distinguishable by eyebrow, badge and position. | Active |
| Email pitch rewrite | The owner felt the benefit-first headline ("Never miss what happens next") did not tell the user what connecting their inbox gives them; the proof cases were too niche (specific scenarios) and too few (only 3). | Lead with the exchange — connect your inbox, we do the work. Use 6 generic capability statements covering the full job search lifecycle. Remove ticks and connecting lines from the proof card. Weave in "unmatched" and "assistance" as marketing power words. | Active |
| Home composer placement | The Ask AmbitionBox composer sat in the page body above a separate nav bar; the owner asked for the Cleo arrangement, where it is part of the navbar. | Chips, ask pill and the three tabs form one fixed bottom cluster on the canvas (MOB-HOME-001). The pill is one slim line; the grounding line moves into the assistant sheet. Accepted cost: ~212px of permanent chrome, in exchange for Ask being reachable from any scroll position and any state. | Active |
| Home page tail | The owner asked what the lone "Add an interview manually" link below the carousel was for, and had it removed. | Home has no page-level link for it. The capability index in the assistant sheet is the single entry point; the sheet's honest "manual entry isn't wired up in this prototype" disclaimer is unchanged. | Active |
| Home why row | "Why this is first" restated in a separate row what each carousel card already says about its own position. | No standing why row on Home. The card reasons carry the arbitration, and the assistant still answers the why question when asked. | Active |
| Home live-email row | The "15 organised · 3 need you" row repeated what the actionable sequence below it already showed. | Home has no live-email row. The greeting owns the only count on the screen and counts the sequence; Tracker owns the organised-inbox count. | Active |
| Home tab label | "Matches" was product vocabulary rather than the user's. | The third tab reads **Jobs** and takes a compass rather than a magnifier, so it does not read as a second search field. Route unchanged at `/matches`. | Active |
| Bottom cluster field | Content showing through the translucent tab bar read as clutter. | The cluster's field is solid canvas with a short fade scrim above it, not a blur. | Active |
| Home card density | The carousel card read as too big and cluttered. | Card headlines take the action-title step (17px), not a display step; metadata sits at 10–10.5px and padding tightens accordingly. A card holds a claim, not a headline. | Active |
| Quote of the day | The owner asked for a sleek, premium quote of the day below the actionables, explicitly not in a container. | One quotation on the bare canvas under the sequence: an oversized pale serif mark, the line at 16.5px/500 on a ~19em measure, and a rule-led attribution in small letterspaced caps. No surface, no border, no shadow. Attributions are checked; no paraphrase is presented as a quotation. | Active |
| Home eyebrow | The owner asked whether the eyebrow should carry a time instead of "Your job search". | **Thursday · 9:41 am** — a fixture time, not a device clock. It agrees with the greeting and stays inside the Thursday the fixtures assume; a real clock would say the wrong day and could open a demo on "Good evening". | Active |
| Bottom cluster shape | The owner asked for the cluster to read as a white sheet that could be swiped up (MOB-HOME-001, Cleo). | White, 28px rounded top, lifted by a shadow, with a grab handle. Chips take a hairline and the ask takes a recessed tinted field, because white-on-white disappears. The handle opens the assistant — the affordance does what it looks like it does. | Active |
| Carousel indicator | The dots and "1 of 4, swipeable" counter were removed. | The peeking neighbour card is the affordance. Do not label a sequence the user can already see. | Active |
| Quote and eyebrow case | Quote of the day needed an all-caps heading and air above it; the eyebrow needed the same treatment. | `QUOTE OF THE DAY` above the mark, with the sequence given clear separation; the eyebrow is set in caps as metadata. | Active |
| Home wordmark | The AmbitionBox wordmark on Home should be all black. | On Home the wordmark is one ink; the blue "Box" treatment stays app-wide elsewhere. The mark carries the brand colour, so the header does not compete with the pick for the eye. | Active |
| Home arrival copy | The first-arrival line was not warm — it read as a status report to someone who had just landed for the first time. | Open with arrival, not with setup confirmation: "You're all set up. I've read through your application email — 4 things need you, most urgent first." Keep the count and the ordering promise. | Active |
| Home card identity | The cards all looked the same and read flat. | Every card carries a 28px tinted category mark. Coloured = something happening to you (blue reply, violet interview, green offer, amber Tracker item); slate = something to choose (roles, connect). A queued role takes a quiet CTA. Size and elevation stay uniform — only identity varies. | Active |
| Home chips | The suggested chips should be more generic pitches. | Four stable pitches for what the assistant is for, identical in every state; the carousel already carries the proof that Home adapts. Each lands on a real answer branch. Supersedes the "prompts are derived from state" clause. | Active |
| Assistant mark | The sparkle icon on the composer looked cheap, not premium. | The AmbitionBox mark replaces it, in the dock pill and the sheet. A sparkle is the generic sign for "an AI did something", which context/UI.md rules out by name. | Active |
| Ask pill motion | The owner asked for a typewriter effect showing what can be asked. | The pill cycles example questions as a placeholder. The typed line is aria-hidden; the contract label is the accessible name. Under reduced motion the contract label is drawn instead, with no caret. | Active |
| Quote presentation | The quote section needed to look premium. | A hairline the width of the text column separates it as the page's footer, and the quotation is set in a serif — editorial content, the one place on Home where the voice is not AmbitionBox's. Deviation contained to this block. | Active |
| Home arrival sources | The arrival line credited only the application email, though Home had also ranked roles from the reviewed preferences. | The greeting names everything the screen drew on: "I've been across your application email and your ranked roles". Home is the screen that has seen everything; the line has to say so. | Active |
| Home chips content | The chips should name concrete AmbitionBox capabilities — monthly in-hand, am I being paid fairly, compare companies — not vague planning questions. | Five chips on AmbitionBox's own territory: pay fairness, take-home, company comparison, role fit, interview readiness. Each is answered from real fixtures. The take-home answer names the missing fixed-pay split instead of inventing a number. | Active |
| Home arrival copy, third pass | Two drafts were rejected for describing what the system had done rather than what changes for the reader. | Relief territory: "You won't have to go looking any more. 4 things need you today — most urgent first." Covers every source without reciting an inventory. Relief is for arrival only; returning visits keep the plain line. | Active |
| Home card composition | "No card looks different, all look the same" — uniform size had been applied to the middle of the card too. | Six shapes, one shell: message, date block, figure, stat columns, task, setup step. Size, radius and elevation never vary; if a shape needs them to, the treatment is wrong. | Active |
| Job card content | Each job should show basic metadata, rating, match %, AmbitionBox Est. Salary, one line on culture, and how ready the profile is. | The card carries all six. Five already existed; AmbitionBox Est. Salary is new fixture data (`estSalary`, `estBasis` in `data.js`). | Active |
| Job salary sourcing | The AmbitionBox estimate should sit alongside the posted range, not replace it. | Two figures, two labelled sources, never blended: the employer's posted range beside AmbitionBox's estimate, with the sample the estimate came from named beneath. An estimate without its n is an opinion. | Active |
| Job detail scope | Keep Juspay as the only openable job for the demo. | The other three cards stay closed and do not pretend otherwise — no arrow, no tap target. No review, salary or interview data is invented for Zeta, PhonePe or Groww. | Active |
| Job card material | The card predated Home's material work and ran at 8-10px, roughly two steps under the documented scale. | Rebuilt in Home's material: white surface, soft shadow instead of a hard border, type on the context/UI.md scale, facts grouped by the question each answers. | Active |
| Job card design | The card should follow the design already built in `prototype/matches-2b.html` and highlight the AmbitionBox core. | Ported: dark source strip carrying the match score, identity row with the locked rating tag, then a Highlights list (pay / culture / profile fit) as the card's spine. Each highlight is a title plus a meta line naming where the claim came from. | Active |
| Company news signals | The mock showed funding and news rows sourced to a named publication. | Not built. Neither prototype has them and no fixture supports them; attributing invented funding news to a real outlet about a real company is a different kind of claim from demo ratings. Three fixture-backed highlights ship instead. | Active |
| Bottom nav field | The nav bar should be white. | White app-wide. On Home it was already white inside the dock sheet; the standalone bar now matches. | Active |
| Screen header | The header should carry the AmbitionBox wordmark only, with a profile touchpoint beside the bookmark. | One shared `.app-header` on Home and Jobs: wordmark left, screen tools right, avatar as the door to Profile. The wordmark is one ink wherever it heads a screen. | Active |
| Sources row | The sources row should be tappable and open a bottom sheet listing the job boards. | The whole strip is the control, not a trailing ⋯ button. The sheet lists one row per source with its state, and names a not-connected board rather than hiding it — "what is this actually watching" is why the sheet is opened. | Active |
| Assistant chrome | Should the AmbitionBox chat or Explore appear on Jobs? | The dock is app-wide chrome, with a context-aware label (`Ask AmbitionBox about Juspay`). Explore stays dropped everywhere — the capability index lives inside the assistant sheet. | Active |
| Saved jobs on Home | Should Home carry a bookmark too? | No. Saved jobs are a Jobs-tab concept; Home is what needs you today. Each header carries only what its screen does. | Active |
| Job detail design | Asked three times: tapping Juspay must open the design in `prototype/job-detail-1b.html`. | Ported. Identity with the locked rating tag, the two action pills, the Preference match card with its brand glow and ok/warn/neutral chips, and Profile Readiness as a segmented meter over fifteen requirements with three openable groups — what already fits, what to strengthen, what may hold you back. A flat bar said "10/15" and stopped; this says which ten. | Active |
| Jobs intro block | The "Curated for Arjun / The roles worth your time" block was removed. | Gone. The feed's own control anchors the screen instead. | Active |
| Jobs feed toolbar | The role count was replaced with what the control actually does. | "Sort jobs by" beside the sort control. A count nobody acts on is not a label. | Active |
| Job card interaction | The whole card should be tappable, and Save should sit beside the CTA on the left. | The article is the target, with the inner controls stopping propagation. Save is a round button to the left of View job; it left the identity row, which freed that space for the score. | Active |
| Match score presentation | The score was not coming out — position or treatment. | It left the dark strip, where it read as a caption, and became a figure in brand ink sitting with the identity it judges. | Active |
| Feed depth and edge cases | Ten more listings, including cases where AmbitionBox insight is missing. | 14 listings. Four lack depth: no salary estimate, no culture read, or neither. Missing insight is drawn as absence — quiet ink, neutral tile, and a line saying why — never as a finding. Cards without a detail screen say so instead of offering a dead action. | Active |
| Assistant mark | The AI orb from the 1B prototype should be the mark used across the flow. | `public/ai-orb.svg`, one glyph everywhere the assistant speaks: the dock pill, the assistant sheet, the job-detail dock. Replaces the AmbitionBox favicon that was standing in for it. | Active |
| Job card CTA | Fill the View job CTA, and give every card the same CTA rather than a prototype disclaimer. | View job fills the row beside Save, and every card carries it; where no detail screen exists the button is simply disabled. The card face no longer announces the prototype's seams. | Active |
| Missing highlights | If there is no data, do not show the highlight. | Rows with no data are left out. An empty row still takes the reader's attention and teaches nothing. | Active |
| Pay highlight shape | Show one salary carrying an AmbitionBox Estimate badge, with the employer's posted range as supporting meta; remove the row when neither exists. | The estimate is the figure and wears the badge — that is what keeps it legible as an estimate rather than a quoted fact. The posted range sits in the meta line. No estimate: the posted range becomes the figure. Neither: no row. | Active |
| Jobs search and filters | The Jobs tab needs search and filters at the top. | A search field over role, company and city, plus Remote / Hybrid / ₹30L+ / Saved filters. They filter the real list — a chip that only looks like a filter is worse than none. | Active |
| Jobs ask pill | The pill should ask search-shaped questions. | "Show me remote roles only", "Which of these pays above ₹30L?", "Who is hiring for payments?", "Which role fits me best?" | Active |
| Job detail dock | Keep the 1B dock on job detail and its flow. | The dock replaces the sticky CTA: orb, label, send. Its destination is whatever genuinely comes next — the assistant while gaps remain, prep once an interview exists, Home once the work here is done. | Active |
| Job detail padding and CTA | Left/right padding was broken and the See match breakdown row was missing. | The 1B cards carry their own gutter margin; `page-pad` on top of that was double-padding them. The match-breakdown row is back and opens the assistant. | Active |
| Applied roles in the jobs feed | Why show jobs already applied to? | A role with a live application is not a job to find — it is an application to track, and Tracker owns it. Anything appearing in `applications.attention` or `applications.waiting` drops out of the Jobs feed entirely (PhonePe, Zeta, Swiggy). | Active |
| Disabled CTA | The buttons don't disable. | They were disabled functionally but drawn in a paler brand blue, which still read as active. Disabled now means grey fill and muted ink. | Active |
| Bookmark spacing | Remove the extra spacing around the bookmark. | The job card's action classes were named `card-action`, which Tracker already owns with a 55px left margin for its indented rows — the port inherited it invisibly, pushing both buttons inward and stopping the CTA filling. Renamed to `job-action`; the bookmark is a 44px rounded square with no leading gap. | Active |
| Dock handle crash | Tapping the sheet handle on Home broke the app. | The handle passed React's click event straight into `ask(question)`, which then called `.toLowerCase()` on it. It now opens the assistant with an empty question, matching the ask pill. | Active |
| Assistant sheet, AI-native rebuild | The sheet was "bleh". Asked to learn from Natural AI: floating, uncontained rows with grey icons; a bigger rectangular chat input at the bottom; and the menu should read as navigation, not chat. | Split into two halves. Top: an uncontained menu — grey glyph, plain label, air between rows, no card, no chevron — because those rows are destinations. Bottom: a rectangular composer with the question on its own line and mark/grounding/send on a control row beneath it, with the chat suggestions sitting directly above it. Dropped the mark block, the "Ask AmbitionBox" heading and the grounding paragraph: three pieces of chrome before anything usable. | Active |
| Capability hints | — | The per-row hint described what each destination was, which the label already said. Rows now carry a live fact instead (`Tuesday 11:00`, `₹28L`) and only when AmbitionBox actually knows one, so the menu shows state rather than marketing. | Active |
| Jobs feed gating | Arriving at Jobs from the bottom nav sometimes showed "some other thing" — a near-empty screen. | The nav was fine; the feed was gated on `naukriConnected`, so arriving with any other preset left two hand-picked rows. Before Naukri connects the feed now shows everything AmbitionBox and company careers already know about; what connecting Naukri adds is the Naukri-sourced listings. 8 cards instead of 2. | Active |
| Evidence flow | The job-detail chat pill should lead into the résumé-gap flow as it works in `prototype/job-detail-1b.html`. | Ported. Four requirements, one question at a time, each answer classified before anything is claimed: negation first (so "I never did that" can never become evidence), then complete, then on-topic (one counter-question), otherwise restate. Receipts describe only what was said; an explicit no leaves the requirement unclaimed rather than quietly softened. | Active |
| Assistant naming | The assistant screen header still read "Career Copilot". | Now "AmbitionBox", per the standing naming rule. | Active |
| Suggestion chips inside the sheet | Not needed once expanded. | Removed. The dock already shows them on the way in; repeating them inside spent the composer's headroom restating the entry point. |Active |
| Composer signature | The line under the field should say "AmbitionBox Career Intelligence". | It does. The assistant signs the field it answers from; the state-dependent grounding sentence that sat there restated what the answer already qualifies. | Active |
| Menu rows | Remove the live facts, add more AmbitionBox options. | `Tuesday 11:00` and `₹28L` are gone — the cards already carry them, so in a menu they read as clutter rather than as news. A row is now a label and a destination, nothing else. Two rows added, both routing to real screens: Track every application (Tracker) and What AmbitionBox knows about me (Profile). Seven rows, every one still goes somewhere that exists. | Active |
| Sheet height | Make it a full-page bottom sheet. | `100dvh` with square corners, inset to the shell above 700px. The stage between the menu and the composer owns the scroll — a full-height sheet that scrolls itself gets two scrollers. | Active |
| Job boards sheet | It should be a bottom sheet, list only real job boards, offer Connect for the ones that are not, and drop the Done button. | `bottom` on the shared Sheet — without it the desktop media query centres it and it reads as a modal. AmbitionBox is not a board (it is the intelligence layer applied on top) and recruiter email is an inbox, not a source of listings; both removed. Naukri and company careers are connected; Indeed, LinkedIn and Wellfound carry a Connect action. Close is the only exit. | Active |
| Evidence flow, second attempt | The 1B flow is a chat interface, and I had built a card form instead — raised repeatedly. | Rebuilt as the thread it is: header with the assistant's identity, a four-segment progress bar, the findings card opening the conversation, question cards, the user's own words in their own bubble, receipts inline, and a composer pinned at the foot with the demo chip. The classification order is unchanged. | Active |
| Assistant sheet header | Back on the left, hamburger on the right showing past chats. | The sheet carries its own header now instead of the shared floating Close, and the grab handle is hidden — a full-page surface is not something you drag. Back is a real back: from a thread or from history it returns to the menu, and only leaves once the menu is showing. | Active |
| Strengthen my application | Change it to résumé. | "Tailor my résumé". The old label named the outcome and hid the artifact; the destination produces a résumé. | Active |
| Tools in the menu | Show tools too — in-hand calculator, gratuity calculator, Am I being paid fairly. | A second group, labelled, because tools answer in place while the rows above leave the sheet. All three route through `homeAnswer` against the same fixtures as every other answer, so they return Arjun's numbers rather than an empty form. Gratuity needed a new branch: it turns on basic pay and single-employer tenure, neither of which AmbitionBox holds, so it states the rule, names both gaps, and leads with the five-year vesting cliff — the part that actually changes a job decision. | Active |
| Menu order | Check the order. | Re-ordered to the funnel the rest of the demo is built in: discover, evaluate, apply, track, interview, offer. Track had been after Offer, which put the everyday screen behind the once-a-search one. "What AmbitionBox knows about me" moved last — it is about the assistant, not the search. | Active |
| Job card CTA state | Do not draw the CTAs as disabled. | Every View job renders in its normal state. A greyed button on ten of eleven cards made the feed look broken rather than unfinished; only Juspay has a detail screen, so only Juspay responds. | Active |
| Profile Readiness CTA | 1B closes the readiness card with its own action row and ours stopped at the groups. | Added, with 1B's label — the card that names the gaps is the one that offers to close them. The dock changed to "Ask about this role" so the screen does not carry the same label twice; once the gap work is done the dock becomes the step onward. | Active |
| Résumé handover screen | Revisit it with what the rest of the UI work has taught us. | The screen exists to hand over a file, so Download is the only primary and Return to job is a quiet text link. Above it: the readiness figure with the same segmented meter used on job detail, then "Changes in this version" naming the four requirements that moved, then the document. The paper preview stops at its content instead of holding a screenful of empty page. | Active |
| Scroll affordance | Add a fade around the middle so users know the product list scrolls; then: make it longer so there is no doubt. | 170px — around three rows deep. At 96px only the last row softened, which still read as a list that had simply ended. Eased through four stops rather than one linear ramp, which bands visibly at that length. Measured rather than assumed: it renders only while something is genuinely below the fold, so a list that fits shows no fade at all. | Active |
| Dock chips | Remove the suggestion chips from the Home navbar too. | Gone. The dock is handle, pill, tabs — 178px, down from 236px. The pill already types an example, so the chips were a second row of chrome offering questions nobody had asked for yet. `PromptChips` survives for the three legacy directions. `tests/onboarding.spec.js` had used a chip as its entry point and now types the same question into the composer instead. | Active |
| Space above the ask pill | Too much padding above it. | Cluster gap 12px → 4px; it had been sized to separate two stacked rows that no longer exist. The handle keeps its 44px height — that is the touch-target floor, not spacing — and gave up its bottom padding instead. Home's `padding-bottom` and the design switcher's offset were re-measured against the shorter dock. | Active |
| Job detail linking | The chat drawer / capability sheet was not opening from job detail, and some links went to the wrong place. | Audited every control on the screen. Three were wrong: **Apply on Naukri** and **See match breakdown** both opened the résumé chat, and the dock navigated instead of opening the assistant. Now: Apply opens a sheet stating that applying happens on Naukri and AmbitionBox never applies on your behalf; See match breakdown asks the assistant why this matches; the dock opens the assistant drawer, as it does on Home and Jobs. The forward step moved onto the readiness card, which is where the gaps are named. | Active |
| Assistant sheet portability | The drawer lost its spacing off Home. | Like the dock, it now carries its own scale instead of borrowing `.home-screen`'s custom properties, which resolve to nothing on any other screen. Exported from Home rather than moved — it is wired to Home's answer engine and relocating that would risk the most contract-bound screen in the app. | Active |
| Contextual nudges | Add contextual nudges at the top of a screen based on its context. | One line, derived from state that is true right now, with the single action that resolves it — dismissible, and absent when there is nothing specific to say. Job detail: unevidenced requirements, then the booked round, then the live offer. Jobs: a waiting recruiter, then the missing Naukri connection, then how many roles already sit in Tracker. | Active |
| Card colour by importance | Categorise the Home cards by colour — offer green, interview violet, others too. Strava as reference. | Live events take a saturated field (offer green, interview violet, reply indigo, update terracotta); ranked roles and Connect stay white. Deep and muted rather than vivid, since these sit beside the brand blue. Promotes a mapping that already existed on the 28px marks and in `priority-card--offer/interview`. Does not re-introduce the hero: colouring by *category* privileges nothing, so the sequence stays equal and order still does the ranking. | Active |
| Tracker chrome | Tracker was the only tab on the bordered `Topbar`, with a Bell that had no handler and no avatar, and the only tab rendering a bare `BottomNav`. | Tracker uses the app chrome: `.app-header` with the avatar as the Profile door, and the shared `AssistantDock`. The assistant is reachable from every tab. | Active |
| Tracker Closed | Closed counted 8 and opened onto a dashed panel that listed nothing — there was no `applications.closed` array at all. | Closed lists all eight, as quiet rows with no CTA and no match score. A stat that is a filter must open onto something real. Closed companies stay disjoint from the Jobs feed. | Active |
| Tracker search | Search was a button opening a sheet that held a second copy of the list. | Search is an in-page `.job-search` field, the same shape the Jobs feed uses, and it filters across every section rather than the selected one. Add sits beside it. | Active |
| Tracker Board | Two views of one set disagreed: List said 8 closed, Board said 6, because the stage counts were hand-typed literals. | Board keeps its 5-column form (owner instruction, 2026-08-19) but derives every count from the same arrays List uses. An application appears in exactly one column. | Active |
| Jobs tab consistency | Arriving on Jobs showed a full-bleed Connect Naukri card and no sources strip, so the tab looked like a different screen depending on where you came from. | The connect card is gone — the strip and the nudge already carried the same offer, and the boards sheet now starts the Naukri flow from its own Connect row. The strip is always present and counts what is actually connected (1 or 2 of 5) rather than asserting a fixed number. The screen's structure no longer changes with journey state; only the counts and the nudge do. | Active |
| Seeing every Home state | Wanted every state visible at once to show a room — and wanted it **inside the demo**, not as a separate document. | `/states`, linked from the demo launcher. Live iframes rather than screenshots, so the board cannot go stale and any tile can be opened and driven mid-presentation. The one route that renders outside the phone shell. A `firstopen` preset was added so the onboarding-handoff state is URL-addressable like the other six. | Active |
| Nudge scope | The PhonePe recruiter nudge appeared on the Jobs tab. | A nudge has to be about the screen it sits on. The recruiter reply belongs to Home, and PhonePe is filtered out of the Jobs feed as an applied role — so it was pointing at something not even on the page. Jobs nudges are now about the feed: where listings come from, and why some are missing. | Active |
| Tracker summary | "This looks bad in UI (add sources Naukri, Email)" | The summary is one surface: period, total, Synced; then an internal divider and a **Tracked from** row naming Gmail, Naukri, and Added by you with counts. Provenance shares the border rather than floating in a second card. | Active |
| Tracker model | "Remove do next from tracker, just show what we have in kanban, just in a list format." | One model only: the pipeline. List renders the stages vertically, Board renders the same stages horizontally, both from one derived array. The Do Next section and the three-way stat filter are gone; attention lives in the card, not in a duplicate section. | Active |
| Tracker card | "The card UI for job in terms of Typography and everything is so different here." | The application card is typeset against `.job-id__*` in the Jobs feed: company 13px/800, role 15px, metadata 11px, full-pill 44px actions, same `.job-score` match block. The old card made the role the smallest line on the card. | Active |
| Pipeline stages | "I think one category should be shortlisted as well" | Six stages: Applied, Recruiter review, **Shortlisted**, Interviewing, Offer, Closed — defined once in `applicationStages`. "Recruiter is reading you" and "recruiter picked you" are different news. | Active |
| Tracker moves | "How do i move an application from one step to another?" | Every card in both views carries **Move**, opening a stage picker that writes to `journey.applicationStages`. Not drag-and-drop: a six-column board on a 430px phone has no honest drop target, and the affordance must exist in the list too. | Active |
| All-clear contradiction | Setting every card aside left "3 things need a look" above an empty rail. | The dismissed list moved from the carousel up to Home, so the greeting counts what the rail actually shows, and `introLine` gained a zero branch. The carousel's own empty line was removed — the greeting owns the only count on Home. The leave animation had to stop using AnimatePresence to make this work: once removal came from a parent re-render, presence tracking desynced and the dismissed card stayed mounted at full opacity. It is now a CSS class plus a timer. Regression-tested. | Active |
| Tracker chips | "All the list options should also be present at the top as chips." | A chip row above the list carries All plus every stage with its count, in the Jobs feed's chip shape; selecting one filters to that stage. | Active |
| Two shortlists | "In the tracker i think shortlist is the first step no?" | "Shortlist" was doing three jobs (saved roles, recruiter picking you, a likelihood chip). The user's own saved roles now lead the pipeline as **Shortlisted**; the recruiter stage is **Recruiter shortlist**; the chip says **Outlook**. Saved roles are not applications, so they stay outside the 15 and are named beside the headline. | Active |
| Section disclosure | "Do we also provide an option to expand and collapse a section" + "The sections are not coming out." | Each stage is a banded 52px disclosure header that visually owns its cards — it was a quiet 13px label on the same canvas as the cards. Closed starts collapsed; every other stage starts open. | Active |
| Board scale | "The UI in the board mode looks so much smaller suddenly." | Board cards use the list card's type scale (13px company, 12.5px role, 12.5px pill actions); they had drifted to 8–9px, so switching views felt like the app had zoomed out. Columns widened to min(86vw,330px) to hold it. | Active |
| Unfounded signals | "You have added Outlook, looks promising, but where and how are we calculating this shit?" | Nothing calculated it — three hardcoded adjectives in the fixture with no source and no consumer but the JSX. Removed. Every signal on a Tracker card must trace to a field (preferences, scanned email, `journey.readiness`, entry source). Do not reintroduce an outcome forecast without real applicant-volume or funnel data behind it. | Active |
| Board/list parity | "The match % UI in list is so different from what it is in board." | Board renders the same `ApplicationCard` as List rather than a card of its own. The two had drifted twice — type scale, then the match score (`.job-score` block vs grey chip) — so the component is now shared, the way `grouped` is shared for counts. | Active |
| Contextual insight supply | AmbitionBox runs on contributed reviews and interview experiences; Home knows when to ask. Review of the current employer on a calm day; interview questions right after the round. | Built the review ask as a new card type: dotted, unfilled, calm days only, always last, and excluded from the greeting's count. Subject is the current employer, so there is no rejection-bias problem. Sheet is honest that the prototype does not submit. | Active |
| Where the ask lives | Should it be in the carousel? Settled as: yes, but as a visually distinct card with a dotted border — something we want, not something ranked for you. | The dotted border does the semantic work, which is what resolved the objection that a supply card in the queue breaks the arbitration contract. Solid = we ranked this for you; dotted = we are asking. Reusable vocabulary if the interview-experience ask ever needs a card. | Active |
| Interview experience ask | Pitch is reciprocal — tell us the questions and we will prepare you for them next time. | Built on `interviewDone` / `?preset=postinterview`. Solid card, not the dotted ask, because it is a real task. Two-step sheet: outcome first (the part that serves the user), questions only after. | Active |
| Post-interview copy | "How did Tuesday go?" — a day of the week is not an interview, name it. | Corrected before shipping. The card now reads "How did your Juspay interview go?" over `Juspay · Senior Backend Engineer · Round 1 of 4`, with the round, interviewer and slot read off the fixture. | Active |
| Mood check-ins | Considered a "how are you feeling lately" touchpoint. | Dropped. A stored feeling the app cannot act on is extraction dressed as empathy, it is a retention mechanic against "designed to be deleted", and it raises a duty-of-care question. The underlying need is better served by inferring from behaviour and offering a pause. | Closed |
| Contextual nudges, corrected | I built banner nudges at the top of Jobs and Job Detail. That was a misread — the ask was contextual ordering **inside the capability sheet**. | All top-of-screen nudges removed. The capability index is now re-ranked by where you opened it from: Job Detail lifts "Tailor my résumé", Jobs lifts "Find roles that fit me", Tracker lifts "Track every application". Journey state outranks screen — a live offer or a booked round lifts those rows wherever you are. Nothing is added or hidden; the rest keeps its baseline order so the menu never feels reshuffled. | Active |
| Naukri connection state | Connecting through Naukri still showed it as unconnected on Jobs. | `naukriConnected` was only ever set by the connect flow on the Jobs screen itself; onboarding's Naukri path sets `onboardingNaukriImported` instead. Anyone who imported their profile at sign-up arrived being told Naukri was not connected and offered a flow they had already completed. The Jobs screen now derives one connection state from both flags and uses it for the feed, the strip and the boards sheet. | Active |
| Job board sources (MOM, 20 Aug) | Stakeholder MOM: pull from boards the family already has, and drop the two with the worst terms-of-service exposure. | The Job boards sheet now reads Naukri (connected) · Company careers (indexed) · iimjobs · Hirist · Instahyre. Indeed, LinkedIn and Wellfound removed. iimjobs and Hirist are InfoEdge-owned, so the sources story becomes "we start with inventory the family already has", which is also what our own terms-safe ingestion claim requires. Naukri must stay first in the list: `golden-path.spec.js` clicks the first Connect button. Both curation strings in Onboarding and the deck image alt text were updated to match. | Active |
| First-open value proposition (MOM, 20 Aug) | Stakeholder MOM: the first screen after login should carry the pitch, so the sentence a stakeholder reads in the deck and the sentence a user reads on first open are the same. | "Know what they know. And what to do about it." now appears as a visible signature line under the three value points, divided by a hairline rule. The three points are untouched: they are owner-approved (row 23) and asserted in eight tests. The h1 had been sr-only since row 21 removed the redundant headline layer, so raising the old text in place would have reinstated the layer that was rejected. Placing it after the points instead keeps them carrying the pitch and lets the value proposition sign it off. **Proposed, not approved** — the placement is the part to review. | Proposed |
| Naming | Pranoy, 2026-09-10: the product is pitched as **NORTH** and NORTH is now the app. | NORTH is the agent — the assistant, the thing that ranks, drafts and moves cards. **AmbitionBox stays the evidence it cites**: ratings, salary estimates, review counts and the parent byline keep the name, because an agent with a proprietary data source behind it is a stronger claim than one without. This supersedes "brand the experience simply as AmbitionBox" for the product surface only. | Active |
| Brand mark | A logo and concept were asked for alongside the rename. | The mark is a compass needle: north half lit, south half in brand blue, a pivot punched through the waist. The concept is **"the needle settles"** — NORTH reads every scattered signal in a job hunt and comes to rest pointing at one thing. That settle is the signature motion and stands in for every thinking state. The wordmark is caps with wide tracking, which is what reads as an instrument rather than a four-letter noun. | Active |
| Pipeline | Five stages, not seven: Applied, Interview scheduled, Offer, Ghosted, Rejected. | Shortlisted leaves Tracker (a saved role is a Jobs concept). Recruiter review and Recruiter shortlist fold into Applied — a recruiter reading you is a signal on the card, not a stage. Closed splits by cause. Supersedes the 2026-08-19 seven-stage pipeline and the instruction that put saved roles at its head. | Active |
| Ghosting | Pranoy, 2026-09-10: "No reply runs irrespective of the status of the job... under each job card we can mention at what stage was it on." | Ghosted cuts across the pipeline rather than following Offer. A ghosted card keeps `ghostedFrom`, names its origin on its face, and offers a follow-up written for that origin. 45 days from Applied is the owner's number; 7 on an unanswered invite and 10 after a round are defaults until changed. Offer and Rejected never ghost. | Active |
| Interview phases | Interview scheduled needs a before and an after. | One stage, two phases. `pre` is a booked or offered round and opens prep; `post` is a round that happened and asks for a debrief. The card is the same card, and a second round puts it back in `pre`. | Active |
| Flows | Pranoy, 2026-09-10: each Home surface gets its own flow, "in a chat like structure with UI elements where ever applicable", and only for the stages named. | One thread engine, six scripts: reply, prep, debrief, rejection, ghosted, offer. North's speech sits on the canvas; what North produces is a card. The Prep and Offer screens survive as the deep-dive a flow hands you to, not as front doors. Contract in `context/screens/FLOW.md`. | Active |
| Context gaps | "Assistant asks if any thing/context is missing to fill gaps." | Every flow has exactly one turn where North names what it cannot know and asks — the notice period, the slot, how the round went, what the user wants from the move. Inside the flows, never as a card on Home. The gap turn takes a rule rather than a colour: it is the honest part, not the alarming part. | Active |
| Agent moves | Pranoy, 2026-09-10, on making North's Gmail moves visible: "Do that cool". | Every Tracker card names who moved it, from where, and how. North's moves offer an Undo; a user's move says so and is never rewritten. The mark is the needle — UI.md rules sparkles out by name, and whose move this was is what the line reports. | Active |
| Rejection reasoning | "Add a dummy data for now, we can add that later." | The rejection flow's three candidate reasons are placeholder content and say so on screen: *"Example reasoning — the real version reads your debrief, the role requirements, and your profile gaps."* Placeholder reasoning that does not admit it is placeholder is the confident wrong answer this product exists to avoid. | Active |
| Job detail scope | "2-3 more with lighter data." | Three roles open: Juspay (readiness moves with the journey), Zeta (static requirement list), Groww (no requirement list, and the screen says so rather than showing an empty meter). Supersedes "keep Juspay as the only openable job". Everything else in the feed stays closed with no tap target. | Active |
| Home scope | "We remove the things which are not on the mentioned stages." | Home shows one card per flow — a reply, a round, a debrief, a quiet one, a role, a rejection — so it reads as the shape of the whole search rather than an inventory. The contribution card was removed: rating your current employer is not one of the seven, and a dated debrief card meant no day was ever calm enough for it to appear again. | Active |

