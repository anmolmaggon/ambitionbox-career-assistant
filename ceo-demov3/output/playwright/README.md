# Playwright Evidence

- `current/` is overwritten by `npm run capture` and contains the current deterministic implementation evidence at 360px, 390px, and 430px.
- `archive/pre-shared-system/` preserves captures made before the shared design workflow was established.
- Neither folder is design inspiration. Intentional external references live only in `references/` and must be catalogued in `references/CATALOG.md`.
